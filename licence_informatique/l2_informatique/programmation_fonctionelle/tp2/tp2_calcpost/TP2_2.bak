(load "libpile.ss")

(define (calcpost)
  (begin
    (display "--------------------------")
    (display "entrez une expression :")
    (newline)
    (let((e (read)))
     (cond((number ? e)(empiler e)(newline)(calcpost))
          ((equal ? e)(
          
          
          
     
     